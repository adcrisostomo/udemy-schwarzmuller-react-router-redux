/*
    NOTE:
    Linux/Mac allows Aux.js filename, but not on Windows
    because Aux.js is a reserved keyword(?) for Win
*/
import React from 'react'

const aux = props => props.children

export default aux